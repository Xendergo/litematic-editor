[package]
name = "litematic-editor"
version = "0.1.0"
edition = "2018"
authors = ["Xendergo"]
description = "A library for generating and editing `.litematic` files generated by the litematica mod"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
quartz_nbt = "0.2.*"
thiserror = "1.0.*"